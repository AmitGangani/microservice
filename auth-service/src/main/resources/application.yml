server:
  port: 8083

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:h2:mem:testdb
    username: amit
    password: amit

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: mySuperSecretKeyForJwtTokenGenerationMySuperSecretKey12345
  expiration: 3600000